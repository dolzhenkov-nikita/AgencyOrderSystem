meta {
  name: DeleteProduct
  type: http
  seq: 3
}

delete {
  url: {{url}}/api/products/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
