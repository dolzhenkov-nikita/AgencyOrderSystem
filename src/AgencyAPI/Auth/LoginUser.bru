meta {
  name: LoginUser
  type: http
  seq: 2
}

post {
  url: {{url}}/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@admin.com",
    "password": "123123"
  }
}

script:post-response {
  let data = res.getBody();
  if (data.token) {
      bru.setEnvVar("token", data.token)
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
