meta {
  name: UpdateStatusOrder
  type: http
  seq: 2
}

patch {
  url: {{url}}/api/orders/3/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status":"processing"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
